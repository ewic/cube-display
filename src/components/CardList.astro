---
import Card from './Card.astro';

interface Props {
	cards: Array<any>
}

const { cards } = Astro.props;
---

<div class="card-list">
  {cards.map(card => {
    return <Card card={card} />
  })}
</div>

<style>
  .card-list {
    display: flex
  }
</style>